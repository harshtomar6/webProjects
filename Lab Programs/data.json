data = {
	question: "1. A. Create a Java Class called student with the following details as variables in it.\n(i) USN\n(ii) Name\n(iii) Branch\n(iv)Phone.\n Write a Java Program to create n student objects and print the USN, Name, Branch and Phone of these object with suitable headings",
	program: "<span class="import">import java.util.Scanner</span>;

  <span class="specifier">public class</span> student{
    <span class="specifier">private</span><span class="keyword"> String</span> usn;
    <span class="specifier">private</span><span class="keyword"> String</span> name;
    <span class="specifier">private</span><span class="keyword"> String</span> branch;
    <span class="specifier">private</span><span class="keyword"> long</span> phone;

    <span class="specifier">public static </span> <span class="return-type">void</span> main(String[] args){
      <span class="comment">//Instance of Scanner object</span>
      Scanner input = <span class="import">new</span> Scanner(System.in);

      <span class="keyword">int</span> n,i;
      
      System.out.println(<span class="string">"Enter the value of n"</span>);
      n = input.<span class="method">nextInt()</span>;

      <span class="comment">//Array of objects of type student</span>
      student[] s = <span class="import">new</span> student[n];

      <span class="keyword">for</span>(i=0;i< n;i++){
        <span class="comment">//instance of each object</span>
        s[i] = <span class="import">new</span> student();
        System.out.println(<span class="string">"Enter USN of student "</span>+(i+1));
        s[i].usn = input.<span class="method">next()</span>;
        System.out.println(<span class="string">"Enter name of student "</span>+(i+1));
        s[i].name = input.<span class="method">next()</span>;
        System.out.println(<span class="string">"Enter branch of student "</span>+(i+1));
        s[i].branch = input.<span class="method">next()</span>;
        System.out.println(<span class="string">"Enter phone number of student "</span>+(i+1));
        s[i].phone = input.<span class="methid">nextLong()</span>;
      }

      System.out.println(<span class="string">"Details entered by you are:\n"</span>);
      <span class="keyword">for</span>(i=0;i< n;i++){
        System.out.println(<span class="string">"Student "</span>+(i+1));
        System.out.println(<span class="string">"USN -> "</span>+s[i].usn+<span class="string">"\nName -> "</span>+s[i].name+<span class="string">"\nBranch -> "</span>+s[i].branch+<span class="string">"\nPhone -> "</span>+s[i].phone+<span class="string">"\n"</span>);
      }
    }
  }"	
};